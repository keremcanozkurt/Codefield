# Optional. Raises the GitHub API rate limit for repository requests.
# Read on the server only. Public repositories work without it.
# A token with read-only access to public repositories is enough.
GITHUB_TOKEN=
